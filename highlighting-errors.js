// Also does improper indentation
function twoLines(param1, param2,
                 param3, param4) {
                     console.log("FAIL");
                 }

// Weird regex that is valid
var regex = /.*coolRegex[^/]/;
